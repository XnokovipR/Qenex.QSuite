<UserControl x:Class="Qenex.QInsight.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Qenex.QInsight.Views"
			 xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
			 xmlns:behaviors="clr-namespace:Qenex.QInsight.Behaviors"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

	<UserControl.Resources>
		<ResourceDictionary>
			<local:LogLevelStyleSelector x:Key="LogLevelStyleSelector">
				<!-- <local:LogLevelStyleSelector.DefaultLogBackgroundStyle> -->
				<!-- 	<Style TargetType="telerik:GridViewRow" BasedOn="{StaticResource GridViewRowStyle}"> -->
				<!-- 		<Setter Property="Foreground" Value="LightSkyBlue"/> -->
				<!-- 	</Style> -->
				<!-- </local:LogLevelStyleSelector.DefaultLogBackgroundStyle> -->
				
				<local:LogLevelStyleSelector.WarningLogBackgroundStyle>
					<Style TargetType="telerik:GridViewRow" BasedOn="{StaticResource GridViewRowStyle}">
						<Setter Property="Foreground" Value="Orange"/>
					</Style>
				</local:LogLevelStyleSelector.WarningLogBackgroundStyle>

				<local:LogLevelStyleSelector.ErrorLogBackgroundStyle>
					<Style TargetType="telerik:GridViewRow" BasedOn="{StaticResource GridViewRowStyle}">
						<Setter Property="Foreground" Value="Red"/>
					</Style>
				</local:LogLevelStyleSelector.ErrorLogBackgroundStyle>

				<local:LogLevelStyleSelector.FatalLogBackgroundStyle>
					<Style TargetType="telerik:GridViewRow" BasedOn="{StaticResource GridViewRowStyle}">
						<Setter Property="Foreground" Value="DarkRed"/>
					</Style>
				</local:LogLevelStyleSelector.FatalLogBackgroundStyle>

				<local:LogLevelStyleSelector.TraceLogBackgroundStyle>
					<Style TargetType="telerik:GridViewRow" BasedOn="{StaticResource GridViewRowStyle}">
						<Setter Property="Foreground" Value="DeepSkyBlue"/>
					</Style>
				</local:LogLevelStyleSelector.TraceLogBackgroundStyle>

				<local:LogLevelStyleSelector.DebugLogBackgroundStyle>
					<Style TargetType="telerik:GridViewRow" BasedOn="{StaticResource GridViewRowStyle}">
						<!-- Mid-tone blue readable on both light and dark theme backgrounds
						     (pure Blue was unreadable in dark mode). -->
						<Setter Property="Foreground" Value="#3B82F6"/>
					</Style>
				</local:LogLevelStyleSelector.DebugLogBackgroundStyle>

			</local:LogLevelStyleSelector>
		</ResourceDictionary>
	</UserControl.Resources>
	
	
	<Grid AllowDrop="False">
		<telerik:RadGridView
			AllowDrop="False"
			ItemsSource="{Binding LogMessages}"
			ShowGroupPanel="False"
			AutoGenerateColumns="False"
			RowStyleSelector="{StaticResource LogLevelStyleSelector}"
			RowIndicatorVisibility="Collapsed"
			behaviors:GridViewFontSizeRefitBehavior.IsEnabled="True">

			<telerik:RadGridView.SortDescriptors>
				<telerik:SortDescriptor Member="Timestamp" SortDirection="Descending"/>
			</telerik:RadGridView.SortDescriptors>

			<telerik:RadContextMenu.ContextMenu>
				<telerik:RadContextMenu>
					<telerik:RadMenuItem Header="Clear All" Command="{Binding ClearLogCommand}"/>
				</telerik:RadContextMenu>
			</telerik:RadContextMenu.ContextMenu>

			<telerik:RadGridView.Columns>
				<!-- Widths come from the sample text measured with the current theme font (see
				     GridViewFontSizeRefitBehavior) — the grid's own automatic sizing never
				     shrinks back after a font decrease. -->
				<telerik:GridViewDataColumn behaviors:GridViewFontSizeRefitBehavior.WidthSampleText="0000/00/00 00:00:00.000" Header="Timestamp" DataMemberBinding="{Binding Path=Timestamp, StringFormat=yyyy/MM/dd HH:mm:ss.fff}" IsReadOnly="True"/>
				<telerik:GridViewDataColumn behaviors:GridViewFontSizeRefitBehavior.WidthSampleText="Warning" Header="Level" DataMemberBinding="{Binding Level}" IsReadOnly="True"/>
				<telerik:GridViewDataColumn Header="Message" DataMemberBinding="{Binding Message}" IsReadOnly="True" Width="*"/>
			</telerik:RadGridView.Columns>

		</telerik:RadGridView>
	</Grid>
</UserControl>
