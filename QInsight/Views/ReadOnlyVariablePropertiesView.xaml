<UserControl x:Class="Qenex.QInsight.Views.ReadOnlyVariablePropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:q="clr-namespace:Qenex.QSuite.Controls.OverridenWinControls;assembly=Qenex.QSuite.Controls.OverridenWinControls"
             xmlns:vm="clr-namespace:Qenex.QInsight.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance vm:ReadOnlyVariablePropertiesViewModel, IsDesignTimeCreatable=False}">

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <q:QTextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,10,5" Text="Id" FontWeight="SemiBold" />
        <q:QTextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,0,5" Text="{Binding Id}" TextWrapping="Wrap" />
        <q:QTextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,10,5" Text="Name" FontWeight="SemiBold" />
        <q:QTextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,0,5" Text="{Binding Name}" TextWrapping="Wrap" />
        <q:QTextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,10,5" Text="Label" FontWeight="SemiBold" />
        <q:QTextBlock Grid.Row="2" Grid.Column="1" Margin="0,0,0,5" Text="{Binding Label}" TextWrapping="Wrap" />
        <q:QTextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,10,5" Text="Description" FontWeight="SemiBold" />
        <q:QTextBlock Grid.Row="3" Grid.Column="1" Margin="0,0,0,5" Text="{Binding Description}" TextWrapping="Wrap" />

        <q:QTextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,10,5" Text="Size" FontWeight="SemiBold" Visibility="{Binding ScalarPropertiesVisibility}" />
        <q:QTextBlock Grid.Row="4" Grid.Column="1" Margin="0,0,0,5" Text="{Binding Size}" TextWrapping="Wrap" Visibility="{Binding ScalarPropertiesVisibility}" />
        <q:QTextBlock Grid.Row="5" Grid.Column="0" Margin="0,0,10,5" Text="Data Type" FontWeight="SemiBold" Visibility="{Binding ScalarPropertiesVisibility}" />
        <q:QTextBlock Grid.Row="5" Grid.Column="1" Margin="0,0,0,5" Text="{Binding DataType}" TextWrapping="Wrap" Visibility="{Binding ScalarPropertiesVisibility}" />
        <q:QTextBlock Grid.Row="6" Grid.Column="0" Margin="0,0,10,5" Text="Value Size" FontWeight="SemiBold" Visibility="{Binding ScalarPropertiesVisibility}" />
        <q:QTextBlock Grid.Row="6" Grid.Column="1" Margin="0,0,0,5" Text="{Binding ValueSize}" TextWrapping="Wrap" Visibility="{Binding ScalarPropertiesVisibility}" />
        <q:QTextBlock Grid.Row="7" Grid.Column="0" Margin="0,0,10,5" Text="Length" FontWeight="SemiBold" Visibility="{Binding ScalarPropertiesVisibility}" />
        <q:QTextBlock Grid.Row="7" Grid.Column="1" Margin="0,0,0,5" Text="{Binding Length}" TextWrapping="Wrap" Visibility="{Binding ScalarPropertiesVisibility}" />
        <q:QTextBlock Grid.Row="8" Grid.Column="0" Margin="0,0,10,0" Text="Presentation" FontWeight="SemiBold" Visibility="{Binding ScalarPropertiesVisibility}" />
        <q:QTextBlock Grid.Row="8" Grid.Column="1" Text="{Binding Presentation}" TextWrapping="Wrap" Visibility="{Binding ScalarPropertiesVisibility}" />
    </Grid>
</UserControl>
