<UserControl x:Class="Qenex.QInsight.Views.PythonSetupPromptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:q="clr-namespace:Qenex.QSuite.Controls.OverridenWinControls;assembly=Qenex.QSuite.Controls.OverridenWinControls"
             xmlns:vm="clr-namespace:Qenex.QInsight.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:PythonSetupPromptViewModel, IsDesignTimeCreatable=False}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <q:QTextBlock Grid.Row="0"
                      Margin="0,0,0,12"
                      Text="{Binding Message}"
                      TextWrapping="Wrap"/>

        <q:QTextBlock Grid.Row="1"
                      Margin="0,0,0,16"
                      VerticalAlignment="Top"
                      Text="{Binding Hint}"
                      TextWrapping="Wrap"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <telerik:RadButton MinWidth="130" Margin="0,0,8,0" Content="Open Preferences" Command="{Binding OpenPreferencesCommand}"/>
            <telerik:RadButton MinWidth="150" Margin="0,0,8,0" Content="Open documentation" Command="{Binding OpenDocumentationCommand}"/>
            <telerik:RadButton MinWidth="80" Content="Close" Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
