<UserControl x:Class="Qenex.QInsight.Views.VariableWatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:vm="clr-namespace:Qenex.QInsight.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="700"
             d:DataContext="{d:DesignInstance vm:VariableWatchViewModel, IsDesignTimeCreatable=False}">

    <Grid Margin="5">
        <telerik:RadGridView
            ItemsSource="{Binding Variables}"
            AutoGenerateColumns="False"
            CanUserDeleteRows="False"
            CanUserInsertRows="False"
            GroupRenderMode="Flat"
            IsReadOnly="True"
            RowIndicatorVisibility="Collapsed"
            ShowGroupPanel="False">

            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Width="160" Header="Label" DataMemberBinding="{Binding Label}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Width="160" Header="Name" DataMemberBinding="{Binding Name}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Width="80" Header="Id" DataMemberBinding="{Binding Id}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Width="*" Header="Value" DataMemberBinding="{Binding Value}" IsReadOnly="True"/>
                <telerik:GridViewDataColumn Width="90" Header="Logged" DataMemberBinding="{Binding Logged}" IsReadOnly="True"/>
            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
    </Grid>
</UserControl>
