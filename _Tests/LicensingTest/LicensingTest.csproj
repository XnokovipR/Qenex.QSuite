<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0-windows</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <UseWPF>true</UseWPF>
        <AssemblyName>Qenex.QInsight.Tests.LicensingTest</AssemblyName>
        <RootNamespace>Qenex.QInsight.Tests.LicensingTest</RootNamespace>
        <LangVersion>latestmajor</LangVersion>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\..\QInsight\QInsight.csproj" />
      <!-- DOCASNE: test si sam, nezavisle na QInsightu, referencuje serverovou
           licencni knihovnu jen kvuli podpisu testovacich tokenu (CreateKeyPair/Sign).
           QInsight tuto referenci uz nema (anti-tamper, commit 65529fb). Detail viz
           komentar v Program.cs. -->
      <PackageReference Include="Qenex.Licensing" Version="1.0.0" />
    </ItemGroup>

</Project>
