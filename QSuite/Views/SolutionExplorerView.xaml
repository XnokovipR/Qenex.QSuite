<UserControl x:Class="Qenex.QSuite.Views.SolutionExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:vm="clr-namespace:Qenex.QSuite.ViewModels"
             xmlns:local="clr-namespace:Qenex.QSuite.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance vm:SolutionExplorerViewModel, IsDesignTimeCreatable=False}">
    <Grid>
        <syncfusion:SfTreeView
            ExpanderPosition="Start"
            ShowLines="True"
            ItemsSource="{Binding ProjectModules}"
            ChildPropertyName="Children"
            
            ScrollViewer.HorizontalScrollBarVisibility="Auto">

            <syncfusion:SfTreeView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="-8,0,0,0">
                        <Image Source="{Binding Icon}"
                               RenderOptions.BitmapScalingMode="Fant" 
                               Stretch="Fill" Width="24" Height="24"/>
                        <TextBlock VerticalAlignment="Center" Text="{Binding Label}"/>
                        <StackPanel.ToolTip>
                            <ToolTip Visibility="{Binding ToolTipVisibility}">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding ToolTip}"/>
                                </StackPanel>
                            </ToolTip>
                        </StackPanel.ToolTip>
                    </StackPanel>
                </DataTemplate>
            </syncfusion:SfTreeView.ItemTemplate>

        </syncfusion:SfTreeView>
        
    </Grid>
</UserControl>
